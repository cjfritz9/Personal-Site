export interface HeroProps {
  heading: string;
}

export interface ProjectProps {
  title: string;
  thumbnail: string;
  href: string;
}

export interface NavLinkProps {
  text: string;
  fontSize?: string;
}
